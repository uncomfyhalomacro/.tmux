#!/bin/sh

main () {
  random_word=$(shuf -n1 /usr/share/dict/web2)
  pane_name=$(openssl rand -hex 3)
  curdir=$(basename "$PWD")
  tmux new-session -s "${random_word}+${curdir}" \; select-pane -T "${pane_name}"
}

main
